# V6.15 — File OR Folder CV Upload

The recruiter can now choose either:
- Upload file(s): select one or multiple CV files.
- Upload folder: select a folder containing CVs.

Both can technically be selected in the same form and are combined, with
duplicate files removed. Neither input is required individually; at least one
file from either option is required.

No D1 schema changes.
